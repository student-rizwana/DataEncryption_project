# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True

# Database Settings (set to True to use MySQL)
USE_MYSQL=False

# MySQL Database (if USE_MYSQL=True)
DB_NAME=cloud_encryption
DB_USER=root
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=3306

